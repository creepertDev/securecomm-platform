import 'package:flutter/material.dart';

// Production WebSocket URL
const String wsUrl = 'wss://api.inv.works';

// Device key — must match DEVICE_KEY in backend .env
// In production distribute via MDM or secure provisioning
const String deviceKey = 'sc-dk-inv-works-2024-secure';

const String appName = 'SecureComm';
const String appVersion = '1.0.0';

const Color militaryGreen = Color(0xFF1B5E20);
const Color darkBg = Color(0xFF0D0D0D);
const Color cardBg = Color(0xFF1A1A1A);
const Color surfaceBg = Color(0xFF222222);
